# Legacy deviations

Legacy behaviours that look like bugs. Each entry: what the legacy code does, the evidence, and the decision (pending / kept / changed by the user). Nothing here is changed without an explicit decision.
